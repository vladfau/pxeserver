PROMPT 0
MENU TITLE pxelinux.cfg
MENU LABEL ^pxelinux.cfg

TEXT HELP
	You are in pxelinux.cfg/p6 
ENDTEXT
label boot
	MENU LABEL Booth this
	kernel memdisk
	APPEND iso initrd=http://mirror.yandex.ru/altlinux/p6/iso
	TEXT HELP
		Selecting this wiil boot image in: pxelinux.cfg 
	ENDTEXT
label uplvl
        MENU LABEL Back
        MENU EXIT
PROMPT 0
MENU TITLE p6
MENU LABEL ^p6

TEXT HELP
	You are in pxelinux.cfg/altlinux/p6 
ENDTEXT
menu include pxelinux.cfg/altlinux/p6/iso/iso.conf iso
label uplvl
        MENU LABEL Back
        MENU EXIT
